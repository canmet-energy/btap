source 'https://rubygems.org'

gemspec

# The sibling gems are not published to RubyGems — resolve them from this
# repository's checkout rather than the index.
gem 'openstudio-audit', path: '../openstudio-audit'

# Test tooling (this gemspec declares no development dependencies).
gem 'minitest', '~> 5.0'
gem 'rake', '~> 13.0'
